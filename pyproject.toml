[project]
name = "ai-agents-tui"
version = "0.1.0"
description = "A terminal UI for the AI agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai",
    "python-dotenv",
    "textual",
    "pydantic",
]
